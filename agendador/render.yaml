services:
  - type: web
    name: agendador-consultas
    env: java
    rootDir: agendador
    buildCommand: chmod +x mvnw && ./mvnw clean package -DskipTests
    startCommand: java -jar target/*.jar
    envVars:
      - key: JAVA_VERSION
        value: 17